import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'site.yrg.prod.square',
  appName: 'YRG Productions',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
